import React from "react";
import ReactMarkdown from "react-markdown";
const Tsne = () => {
  return (
    <div>
      <ReactMarkdown># ML Technique: T-SNE</ReactMarkdown>
    </div>
  );
};

export default Tsne;
